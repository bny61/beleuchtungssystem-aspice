---
id: JOB-003
created: 2026-08-02T12:15:01+00:00
status: done
target: SYS-REQ-001
target_kind: record
agent: hardware-engineer
relates_to: []
planned_at: "2026-08-02T12:29:35+00:00"
approved_at: "2026-08-02T12:31:15+00:00"
branch: job/JOB-003
result: "Approved plan executed as written, including the Option A decision on the blanking conflict. Nothing committed or pushed. ## What I changed **Created — `05_hardware/`** | ID | Subject | Key values (all plausible examples) | |---|---|---| | `HW-REQ-026` | Enable-gate propagation, energising direction | ≤ 1 ms | | `HW-REQ-027` | Soft start | ≥ 95 % of set point in ≤ 20 ms, ramp ≥ 5 ms | | `HW-REQ-028` | Channel-to-channel skew | ≤ 10 ms | | `HW-REQ-029` | Switch-on inrush | ≤ 8 A, `VBAT_PROT` dip "
---

## Task

refine this requirement for hardware

## Context

SYS-REQ-001 (functional, ASIL B, draft) in 01_requirements/system/SYS-REQ-001.md
Text: When the driver requests low beam via the light switch signal, the lighting ECU shall energise both low-beam channels within 300 ms.
Derived from: CR-001, FSR-002 | Refined by: -

## Plan

The plan is written to `/Users/tugcegulbicim/.claude/plans/this-task-concerns-sys-req-001-rustling-oasis.md`. Nothing in the repo has been touched.

## Plan summary — hardware refinement of SYS-REQ-001 (JOB-003)

**Why it's needed:** `SYS-REQ-001` has no downstream refinement at all — every existing `HW-REQ-001…025` derives from `SYS-REQ-012…020`, `CR-…`, `FSR-…` or `TSR-…`. The low-beam *activation* path was never taken into hardware. Also: the 300 ms of `SYS-REQ-001` is an activation latency, **not** the 300 ms FTTI of `SG-01` — same number, different meaning, and the deliverable will say so.

**The finding that makes this worth doing properly:** during the constant-current driver's soft-start ramp the channel current is below the 150 mA open-load threshold of `HW-REQ-002`, so `SM-01` would false-trip at every switch-on unless the diagnosis is blanked. That is a Golden Thread interaction → 🔍 DEEP DIVE.

**Would create** (`05_hardware/`, existing front-matter format, ASIL B, draft, all values labelled plausible examples):
- `HW-REQ-026` enable-gate propagation ≤ 1 ms (`HW-REQ-019` only covers the de-energise direction)
- `HW-REQ-027` soft-start: ≥ 95 % of set point within 20 ms, ramp ≥ 5 ms
- `HW-REQ-028` channel-to-channel skew ≤ 10 ms
- `HW-REQ-029` switch-on inrush ≤ 8 A, supply dip ≤ 1 V, no `SM-06` undervoltage trip
- `HW-REQ-030` 🔍 `SM-01` blanking 30 ms after enable → start-up detection ≈ 100 ms + 150 ms reaction = 250 ms vs 300 ms FTTI, margin 17 %
- `analysis_low_beam_activation.md` with the budget: gateway 100 ms + bus ≈ 103 ms + SW 10 ms + **HW ≤ 25 ms** ≈ 238 ms of 300 ms. Two thirds is consumed before the ECU sees the request — a `systems-engineer` hand-off, not mine to fix.
- **No new `SM-xx`**, stated explicitly.

**Would amend:** `hw_verification_plan.md` (HV-13 activation timing, HV-14 inrush + "no open-load classification in 1000 switch-ons", DV/PV row, open point), `hw_architecture.md` (owning-HW-REQ column, note on the second `SM-01` detection case), `assumptions.md` (`A-23`, gateway forwards switch position within 100 ms). Then regenerate indexes and reports with `tools/`.

**Deliberately not touched:** `SYS-REQ-001` and `CR-001` themselves; all published values in `HW-REQ-001…025` and `SM-01…06`, in particular `SM-01`'s 80 ms / 230 ms; any diagnostic-coverage number; the `ee_architecture` timing chains; `TC-xxx` records. No commit, no push.

**Decide before I write:**
1. **Scope** — five records as recommended, or a minimal two-record cut (`HW-REQ-027` + `030`, budget inline). KL15 power-on readiness left out on purpose: "request arrives while the ECU boots" is a start-up requirement `SYS-REQ-001` does not state.
2. **ID block** `HW-REQ-026…030` confirmed free.
3. **`SM-01`** — my recommendation is to leave `SM-01.md` untouched and hand the start-up detection case to `safety-analyst`, rather than editing the coverage-bearing record myself.
4. **A-23** — is 100 ms gateway forwarding acceptable as a plausible example, or is there an OEM figure?

## Decisions (from the human, binding for this run)

**As recommended, on all four.**

1. **Five records**, `HW-REQ-026 … HW-REQ-030`, plus `analysis_low_beam_activation.md`.
2. `HW-REQ-026 … HW-REQ-030` are confirmed free — checked against `05_hardware/`.
3. **Do not touch `SM-01.md`.** Hand the start-up detection case to `safety-analyst` as an open
   point. `SM-01` carries the conditional 90 % diagnostic-coverage claim that `OP-15` already
   blocks; a second agent editing it would tangle two open threads, and the coverage number is
   the safety-analyst's to move.
4. **`A-23` at 100 ms as a plausible example**, marked open and needing OEM confirmation, like
   every other assumption here.

Also binding, from the plan's own findings:

- Keep the distinction explicit wherever the number appears: the 300 ms of `SYS-REQ-001` is an
  **activation latency**, not the 300 ms **FTTI** of `SG-01`. Same figure, different meaning.
- Raise the activation-budget finding as an open point for `systems-engineer` rather than
  absorbing it: roughly two thirds of the 300 ms is spent before the ECU sees the request.
- Every numeric value stays labelled a plausible example, and no published value in
  `HW-REQ-001 … 025` or `SM-01 … 06` changes.

**If any of this turns out to be unworkable, stop and say so rather than working around it.**

## Decision on the blanking conflict (second round, binding)

You stopped correctly: 30 ms blanking + the 80 ms of `HW-REQ-009` is 110 ms, and `SYS-REQ-018`
caps open-load reporting at 100 ms. Verified against the records — the breach is real.

**Option A: keep the 30 ms.** Write `HW-REQ-030` as approved, state the resulting **110 ms**
plainly, and raise an open point for `systems-engineer` that `SYS-REQ-018`'s 100 ms cap is
exceeded during the switch-on transient and needs either widening or an explicit exemption for
the start-up window.

Do **not** re-derive the blanking against the 170 mA band. Your Option B is sound engineering,
but it changes the basis of an approved concept on your own authority, and the cap it collides
with belongs to `SYS-REQ-018` — the systems engineer's requirement, not hardware's to design
around. Record the conflict and let its owner resolve it.

So: `HW-REQ-030` states 30 ms blanking and 110 ms start-up detection, and the analysis says in
plain words that this exceeds `SYS-REQ-018` and why, with the open point referenced. Everything
else in the plan stands exactly as approved.
