---
id: SM-01
text: >
  Open-load detection in the low-beam channel via PWM-synchronous shunt current measurement with
  threshold comparison, debouncing, off-phase zero-current self-test and discrimination against
  short-to-battery and commanded current reduction using the channel output voltage and the LED
  driver status; reported to SWC_LightManager for the fault reaction.
type: safety-mechanism
asil: B
source: FSR-001
derived_from: [FSR-001, TSR-003]
allocated_to: [ECU_LightingCtrl, LED_Driver_Stage_1, Current_Sense_Chain]
verified_by: [TC-021]
status: draft
detection_time: >
  <= 80 ms worst case = 2.5 ms PWM synchronisation + 0.1 ms acquisition + 50 ms threshold window
  + 20 ms debounce + 5 ms task latency. Allocated budget cap 100 ms (SYS-REQ-018, HW-REQ-009).
diagnostic_coverage: >
  90 % (plausible example value, not validated) - the claim is CONDITIONAL on HW-REQ-005
  (off-phase zero-current self-test), HW-REQ-006 (channel voltage sense), HW-REQ-007 (driver status
  readback) and HW-REQ-010 (ADC reference plausibility) being implemented. Without them the
  defensible value is approximately 60 %. Confirmation of the number is owned by safety-analyst via
  the FMEDA.
owning_hw_req: [HW-REQ-001, HW-REQ-002, HW-REQ-003, HW-REQ-004, HW-REQ-005, HW-REQ-006, HW-REQ-007, HW-REQ-008, HW-REQ-009, HW-REQ-010]
rationale: >
  Detection time (80 ms) + reaction time (150 ms) = 230 ms < FTTI (300 ms) of SG-01, margin 70 ms
  (23 %). Threshold secured by the tolerance analysis in HW-REQ-001 / HW-REQ-002: guaranteed trip
  below 130 mA, guaranteed no trip above 170 mA.
---

## Context

Golden Thread safety mechanism.

## Change note (phase 3 refinement, hardware-engineer)

- `detection_time` 70 ms -> **80 ms**: the PWM-synchronous measurement adds 2.5 ms synchronisation,
  0.1 ms acquisition and 5 ms task latency. The budget still closes against the 300 ms FTTI.
- `diagnostic_coverage`: the bare 90 % was not defensible. Current-threshold sensing alone leaves
  partial load loss, short-to-battery misclassification and failures of the diagnostic path itself
  uncovered - approximately 60 %. The 90 % claim is now explicitly conditional on four measures.
- Status dropped from `reviewed` to `draft`; re-review required, and the DC number needs
  confirmation by `safety-analyst` against the FMEDA before it returns to `reviewed`.

Analysis: [`analysis_current_sensing.md`](analysis_current_sensing.md)
